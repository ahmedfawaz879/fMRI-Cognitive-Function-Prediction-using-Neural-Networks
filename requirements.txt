# Runtime dependencies, exactly pinned for reproducibility.
# These versions are the ones the full test suite was actually verified
# against (see PR verification notes), on Python 3.12 (Windows).
numpy==2.3.4
pandas==2.2.3
nibabel==5.3.2
matplotlib==3.10.7
scikit-learn==1.7.2
torch==2.9.0
joblib==1.5.2
pyyaml==6.0.3
